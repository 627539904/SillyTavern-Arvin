# SillyTavern-Arvin 项目技术栈分析

本分析旨在帮助开发者快速了解本项目涉及的主要技术栈，并通过文件/目录定位，便于后续魔改和学习。

---

## 1. 前端技术栈

### 主要技术与表现

- **HTML5/CSS3**
  - 结构与样式，页面布局与主题。
  - 相关目录/文件：
    - `public/index.html`
    - `public/css/`
    - `public/scripts/templates/`

- **JavaScript (ES6+) & jQuery**
  - 主体逻辑，事件处理，DOM 操作，AJAX。
  - 相关目录/文件：
    - `public/scripts/`（主逻辑）
    - `public/scripts/extensions/`（各类扩展）
    - `public/script.js`（全局入口）
    - `public/scripts/extensions/tts/`（TTS 扩展）
    - `public/scripts/extensions/assets/`（资源扩展）

- **Webpack**
  - 前端打包与模块化。
  - 相关文件：
    - `webpack.config.js`

- **Live2D**
  - 动态角色渲染与语音联动。
  - 相关目录/文件：
    - `public/assets/live2d/`（Live2D 资源）
    - `public/scripts/extensions/`（如有 Live2D 相关 JS）
    - `mobile-live2d-test.html`（移动端测试）

- **Web Audio/Media**
  - 音频播放与处理。
  - 相关目录/文件：
    - `public/sounds/`
    - `public/scripts/extensions/tts/`

---

## 2. 后端技术栈

### 主要技术与表现

- **Node.js + Express**
  - 主后端服务，API 路由，静态资源服务。
  - 相关目录/文件：
    - `server.js`
    - `src/endpoints/`（API 路由）
    - `src/`（后端工具与中间件）

- **Shell/Bat 脚本**
  - 启动、更新、部署辅助。
  - 相关文件：
    - `Start.bat`、`start.sh`、`UpdateAndStart.bat` 等

---

## 3. 数据与配置

- **JSON 配置**
  - 角色、主题、TTS 预设等。
  - 相关目录/文件：
    - `default/content/`（角色、主题、预设）
    - `public/scripts/extensions/tts/`（部分 provider 配置）

- **YAML 配置**
  - 全局配置。
  - 相关文件：
    - `default/config.yaml`

---

## 4. 特色扩展与魔改方向

- **TTS 扩展**
  - 多种 TTS provider，便于魔改。
  - 相关目录/文件：
    - `public/scripts/extensions/tts/`

- **Live2D 扩展**
  - 角色动态渲染与语音联动。
  - 相关目录/文件：
    - `public/assets/live2d/`
    - `public/scripts/extensions/`（如有 Live2D 相关 JS）

- **UI/UX 魔改**
  - 相关目录/文件：
    - `public/css/`
    - `public/scripts/templates/`
    - `public/index.html`

---

## 5. 学习建议

- 针对不熟悉的技术栈，可结合上述文件/目录定位源码，配合官方文档、社区教程进行针对性学习。
- 推荐优先掌握：Node.js/Express、FastAPI、jQuery、Webpack、Live2D Web SDK、音频流处理等。

---

如需对某一技术栈的深入学习路线或魔改实战案例，可随时指定方向！ 